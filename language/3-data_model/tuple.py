#!/usr/bin/python

# A tuple is an immutable sequence of items
t="one",2,"three"

# When printed, tuples are represented enclosed in parentheses
print "Tuple {} has {} elements.".format(t, len(t))

# empty tuple
t=()
print "Tuple {} has {} elements.".format(t, len(t))

# tuple of one
t="two",
print "Tuple {} has {} elements.".format(t, len(t))

# mutable items in a tuple _can_ be changed

